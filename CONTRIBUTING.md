# Contributing

Something is a local-first reader. The useful work is in `src/core`.

1. `bun install && bun test && bun run dev`
2. Keep React out of `src/core`.
3. Do not commit files under `references/`.
4. Conventional commits: `feat:`, `fix:`, `docs:`, `chore:`.
