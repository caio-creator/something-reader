# Security

Documents never leave the origin. HTML from EPUB is parsed without executing scripts. Imports are capped at 80 MB.

If you find a way to run untrusted document content, email the maintainer privately rather than opening a public issue with a working exploit.
